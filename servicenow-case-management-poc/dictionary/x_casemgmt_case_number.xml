<?xml version="1.0" encoding="UTF-8"?>
<!--
    PROVENANCE - Refine PR Phase 1 native rebuild (supersedes the hand-authored
    origin of this record; see docs/refine-run/PHASE1-REBUILD.md):
    This file is a serialization of the record the PLATFORM ITSELF captured when the
    `number` field on the `x_casemgmt_case` table
    was created through the real Table API / dictionary API. It is no longer a
    hand-authored record definition. The human's PR directive requires all table and
    role-link records to be created by native platform actions so that physical
    storage provisioning and capture side effects fire; that directive overrides the
    Agent Action Plan's CREATE-mode hand-authored origin for this path.
      Captured into Update Set : x_casemgmt_case_management v1.0.0 (native rebuild)
      Update record name       : sys_dictionary_x_casemgmt_case_number
      Update record type       : Dictionary
      Update record target     : Case.Number
      Captured (instance time) : 2026-09-02 19:14:03
    What this changes about any note below:
      - the <sys_id> element carries the identity the platform assigned at creation
        time (14da6c5e938b435009aa70d19dba1078), not a deterministic MD5 of the field identifier;
      - the element order, the action/element/table attributes the platform places on
        the record element, and the platform-managed elements it adds (the calculation
        template, virtual_type, formula, reference_floats, ...) are its own
        serialization, not a formatting choice made here;
      - the <sys_scope>/<sys_package> elements are the platform's own serialization of
        the x_casemgmt scope reference (display_value / source attribute), reproduced
        verbatim; the only sys_id this file introduces of its own accord is none - the
        rebuild resolved the scope by query (sys_scope?scope=x_casemgmt) and let the
        platform write the reference.
    Every statement about semantics, type, constraints, scope exclusivity and AAP
    traceability still holds: each value was read from the live dictionary before the
    rebuild and replayed into the platform verbatim. Only statements about HOW the
    record was authored are superseded.

    ServiceNow Update Set record-definition for the `sys_dictionary` row that
    declares the `number` Auto-number String field on the `x_casemgmt_case`
    custom table inside the scoped application `x_casemgmt`.

    Per AAP Section 0.5.7 (Data-Model Mapping verbatim):
        Field   Type            Constraints
        number  Auto-number     Read-only, format CASE0000001

    Per AAP Section 0.7.4 (Other User-Provided Rules):
        "Auto-numbering format: the case `number` field MUST use format
         CASE0000001 (7-digit zero-padded) and be Read-only."

    This dictionary entry ONLY declares that the `number` field exists on the
    parent table. The auto-population logic (prefix `CASE`, padding 7,
    starting value, format `CASE0000001`) is configured by a separate
    sys_number record in `../numbers/sys_number_x_casemgmt_case.xml`. The
    parent table record `../tables/x_casemgmt_case.xml` connects this field
    to that auto-number generator via its `<number_ref>` element (which
    already resolves to `x_casemgmt_case_number` via `source` lookup). No
    redundant linkage is authored here.

    Semantic source-side reference (read-only, NOT byte-for-byte ported):
      - acm-plugins/acm-default-plugins/acm-case-file-plugin/src/main/java/
        com/armedia/acm/plugins/casefile/model/CaseFile.java line 130-132:
            @Column(name = "cm_case_number")
            @AcmSequence(sequenceName = "caseNumberSequence")
            private String caseNumber;
        ArkCase's `caseNumber` String field on the `acm_case_file` JPA entity
        was sequence-generated by the @AcmSequence annotation backed by a
        dedicated sequence row in the `acm_sequence` table. The ServiceNow
        target replaces both the @Column declaration AND the @AcmSequence
        generator with this dictionary entry plus the sibling sys_number
        record - a single declarative low-code equivalent of the JPA pair.
      - The field name is `number` (lowercase, NOT `caseNumber`, NOT
        `case_number`) per AAP Section 0.5.7 verbatim. This is the
        conventional ServiceNow auto-number field name across the platform
        (incident.number, change_request.number, sc_request.number, etc.).

    Constraints honored (AAP Sections 0.7.1, 0.7.2, 0.7.4):
      - Scope-namespace exclusivity: `<name>` resolves to `x_casemgmt_case`
        within the scoped application, and `<sys_scope>` / `<sys_package>`
        bind THIS dictionary record to the same scope by `source` attribute
        lookup. NO global-scope writes - the OOB tables `task`, `incident`,
        `sys_user`, etc. are never modified.
      - Zero hardcoded cross-reference sys_ids: the `<sys_scope>` and
        `<sys_package>` text-node values resolve by scope source name
        (`x_casemgmt`), not by literal sys_id. The `<sys_id>`
        text-node value is THIS record's own deterministic primary key
        (MD5 of the canonical field identifier `x_casemgmt_case.number`)
        and is NOT a reference to any other record.
      - The concrete scope identifier `casemgmt` is used per AAP convention;
        ServiceNow Update Set imports use a standard XML parser, so the
        scope id must be concrete in the XML before export.
      - THE AUTO-NUMBER GENERATOR IS WIRED HERE, WITH THE `global.`
        QUALIFIER (Defect E). `<default_value>` carries
            javascript:global.getNextObjNumberPadded();
        The `global.` prefix is NOT optional. `getNextObjNumberPadded()`
        is a global-scope function; when the default is evaluated for a
        row on a SCOPED table the expression is resolved in that scope, so
        the unqualified form does not resolve and the field is left empty.
        Root cause of the defect this fixes: on a normal, UI-driven table
        creation the platform's after-insert business rule `Create Default
        Number Maintenance Field` (order 1000, on `sys_db_object`) writes
        this default for you. An Update Set commit applies every payload
        with business rules suppressed, so that rule never runs and the
        default has to be part of the payload - which it now is. Without
        it, `x_casemgmt_case` rows insert with an empty `number`, which
        breaks the portal submit contract (it returns the new case number)
        and the status-lookup contract (it queries by number).
        The default alone is not sufficient: the counter must also declare
        its zero-padding. See `../numbers/sys_number_x_casemgmt_case.xml`,
        which carries `<maximum_digits>7</maximum_digits>`. Together they
        yield the mandated `CASE0000001` format; the default without the
        padding yields `CASE1`.
      - Read-only at the dictionary level (`<read_only>true</read_only>`)
        per AAP Section 0.7.4 verbatim, and preserved by the Defect E fix.
        No internal user, agent, or manager may directly edit this field;
        only the platform's auto-number generator may populate it on
        insert. `internal_type` (string) and `max_length` (40) are likewise
        unchanged by the fix.
      - Mandatory at the dictionary level is FALSE because the auto-number
        generator populates the field server-side on insert; setting
        mandatory to true would block any GlideRecord.insert() that does
        not explicitly include a `number`, contradicting the
        auto-population semantic. (The platform itself ALWAYS guarantees
        a value will be generated.)
      - Unique at the dictionary level is TRUE: belt-and-suspenders against
        any future code path that might attempt to bypass the auto-number
        generator and assign a duplicate value. The platform's auto-number
        generator already guarantees uniqueness; this adds an
        index-backed enforcement layer.
      - Default-sort rank is 1: ServiceNow list views on `x_casemgmt_case`
        default to sorting by `number`, so the most recently created cases
        (with the highest sequential numbers) sit together at one end of the
        list - matching agent / manager workflow expectations.
        `sys_dictionary.defaultsort` is an INTEGER column on this release,
        not a boolean: its own dictionary entry reports
        internal_type=integer, and writing the string `true` was measured
        leaving the stored value untouched without even incrementing
        sys_mod_count, while writing `1` persists. An earlier revision of
        this artifact declared `<defaultsort>true</defaultsort>`, which
        import therefore discarded in silence and left the case list with no
        default sort at all. `../scripts/post_import_remediation.js` carries
        the same rank and verifies it.
      - max_length 40 is generous: the prescribed format `CASE0000001`
        consumes 11 characters; the additional headroom accommodates
        future format extensions (e.g., `CASE-2026-0000001` = 17 chars,
        `CASE-DEPT-NN-0000001` = 20 chars) without a schema migration.
      - choice 0 = free-form String (no choice list), as required for an
        auto-numbered identifier.
      - No reference target: this is a String field, not a Reference field.
        All `<reference*>` elements are empty.
      - No PII: the field stores only synthetic auto-generated identifiers
        (`CASE0000001`, etc.); no real names, email addresses, or
        organizational identifiers are derived from or stored in this
        column.

    Cross-references (AAP Section 0.5.2 dependency ordering):
      - This record MUST load AFTER `../tables/x_casemgmt_case.xml`
        (parent table), since `<name>x_casemgmt_case</name>` references
        the table by name.
      - This record SHOULD load alongside or before
        `../numbers/sys_number_x_casemgmt_case.xml`. The auto-number
        generator's `<table>` field references the parent table by name,
        and the parent table's `<number_ref>` already targets the
        sys_number record by `source` attribute - so the dictionary
        entry, the sys_number, and the table all resolve correctly
        regardless of strict ordering once their `name` columns exist.
      - This record MUST load BEFORE the consumers that read or write
        the `number` column:
          * ../portal/rest/sys_ws_definition_x_casemgmt_case_submit.xml
            (returns the auto-generated number to the requester)
          * ../portal/rest/sys_ws_definition_x_casemgmt_case_status_lookup.xml
            (queries cases by number and returns status/subject/opened_date)
          * ../seed-data/cases/x_casemgmt_case_demo_*.xml (synthetic demo
            cases that may pre-assign numbers `CASE0000001` through
            `CASE0000010` during seeding)

    SINGLE DISPLAY FIELD FOR `x_casemgmt_case` (`display=true`):

      ServiceNow permits exactly ONE display field per table, and `number`
      is it for `x_casemgmt_case`. Every other dictionary entry on this table
      therefore carries `<display>false</display>`.

      This is not cosmetic. When several columns are flagged, every reference to a case - the `Case` column on the task and party lists, and the mandatory `Case` field on their forms
      renders blank; when none is flagged, they render a raw sys_id. An
      earlier revision of this package flagged 13 of the 14 `x_casemgmt_case`
      columns, 6 of 6 on `x_casemgmt_case_task` and 5 of 5 on
      `x_casemgmt_case_party`, and the effect was measured on the live
      instance: `getDisplayValue('case')` returned the empty string and the
      `Case` column showed "(empty)" on every task and party row.

      Normal writes cannot produce that state - the platform clears the flag
      on sibling columns whenever one is set - but an Update Set commit
      applies payloads with business rules suppressed, so the package itself
      is the only thing that can create it. Hence the flags shipped here.
      `../scripts/post_import_remediation.js` carries the same declaration,
      reconciles a table down to this one field, and fails verification if a
      table ends up with any number of display fields other than one.
-->
<record_update>
    <sys_dictionary action="INSERT_OR_UPDATE" element="number" table="x_casemgmt_case">
        <active>true</active>
        <array>false</array>
        <attributes/>
        <audit>false</audit>
        <calculation><![CDATA[(function calculatedFieldValue(current) {

	// Add your code here
	return '';  // return the calculated value

})(current);]]></calculation>
        <choice/>
        <choice_field/>
        <choice_table/>
        <column_label>Number</column_label>
        <comments>Auto-generated case number with format CASE0000001 (4-char prefix CASE + 7-digit zero-padded sequence). Read-only. Auto-population is configured by the parent table's number_ref pointing at ../numbers/sys_number_x_casemgmt_case.xml. Primary user-facing identifier of the case (referenced by portal submission and lookup, seed scripts, dashboards). Replaces ArkCase CaseFile.caseNumber (@AcmSequence + @TableGenerator).</comments>
        <create_roles/>
        <default_value>javascript:global.getNextObjNumberPadded();</default_value>
        <defaultsort>1</defaultsort>
        <delete_roles/>
        <dependent/>
        <dependent_on_field/>
        <display>true</display>
        <dynamic_creation>false</dynamic_creation>
        <dynamic_creation_script/>
        <dynamic_default_value/>
        <dynamic_ref_qual/>
        <element>number</element>
        <element_reference>false</element_reference>
        <foreign_database/>
        <formula/>
        <function_definition/>
        <function_field>false</function_field>
        <internal_type display_value="String">string</internal_type>
        <mandatory>false</mandatory>
        <max_length>40</max_length>
        <name>x_casemgmt_case</name>
        <next_element/>
        <primary>false</primary>
        <read_only>true</read_only>
        <read_roles/>
        <reference/>
        <reference_cascade_rule/>
        <reference_floats>false</reference_floats>
        <reference_key/>
        <reference_qual/>
        <reference_qual_condition/>
        <reference_type/>
        <spell_check>false</spell_check>
        <staged>false</staged>
        <sys_class_name>sys_dictionary</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-09-02 19:14:03</sys_created_on>
        <sys_id>14da6c5e938b435009aa70d19dba1078</sys_id>
        <sys_name>Number</sys_name>
        <sys_package display_value="x_casemgmt Case Management" source="x_casemgmt">82b99028936f74320d74d6f88357a5af</sys_package>
        <sys_policy/>
        <sys_scope display_value="x_casemgmt Case Management">82b99028936f74320d74d6f88357a5af</sys_scope>
        <sys_update_name>sys_dictionary_x_casemgmt_case_number</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-09-02 19:14:03</sys_updated_on>
        <table_reference>false</table_reference>
        <text_index>false</text_index>
        <unique>true</unique>
        <use_dependent_field>false</use_dependent_field>
        <use_dynamic_default>false</use_dynamic_default>
        <use_reference_qualifier>simple</use_reference_qualifier>
        <virtual>false</virtual>
        <virtual_type>script</virtual_type>
        <widget/>
        <write_roles/>
        <xml_view>false</xml_view>
    </sys_dictionary>
</record_update>
