<?xml version="1.0" encoding="UTF-8"?>
<!--
    PROVENANCE - Refine PR Phase 1 native rebuild (supersedes the hand-authored
    origin of this record; see docs/refine-run/PHASE1-REBUILD.md):
    This file is a serialization of the record the PLATFORM ITSELF captured when the
    `priority` field on the `x_casemgmt_case` table
    was created through the real Table API / dictionary API. It is no longer a
    hand-authored record definition. The human's PR directive requires all table and
    role-link records to be created by native platform actions so that physical
    storage provisioning and capture side effects fire; that directive overrides the
    Agent Action Plan's CREATE-mode hand-authored origin for this path.
      Captured into Update Set : x_casemgmt_case_management v1.0.0 (native rebuild)
      Update record name       : sys_dictionary_x_casemgmt_case_priority
      Update record type       : Dictionary
      Update record target     : Case.Priority
      Captured (instance time) : 2026-09-02 19:14:05
    What this changes about any note below:
      - the <sys_id> element carries the identity the platform assigned at creation
        time (f0daac5e938b435009aa70d19dba1015), not a deterministic MD5 of the field identifier;
      - the element order, the action/element/table attributes the platform places on
        the record element, and the platform-managed elements it adds (the calculation
        template, virtual_type, formula, reference_floats, ...) are its own
        serialization, not a formatting choice made here;
      - the <sys_scope>/<sys_package> elements are the platform's own serialization of
        the x_casemgmt scope reference (display_value on both, plus source="x_casemgmt"
        on sys_package only; sys_scope carries no source attribute), reproduced
        verbatim; the only sys_id this file introduces of its own accord is none - the
        rebuild resolved the scope by query (sys_scope?scope=x_casemgmt) and let the
        platform write the reference.
    Every statement about semantics, type, constraints, scope exclusivity and AAP
    traceability still holds: each value was read from the live dictionary before the
    rebuild and replayed into the platform verbatim. Only statements about HOW the
    record was authored are superseded.

    ServiceNow Update Set record-definition for the `sys_dictionary` row that
    declares the `priority` Choice field on the `x_casemgmt_case` custom table
    inside the scoped application `x_casemgmt`.

    Per AAP Section 0.5.7 (Data-Model Mapping verbatim):
        Field      Type      Constraints
        priority   Choice    Low, Medium, High, Critical

    Per AAP Section 0.4.1 (Refactored Structure Planning):
        choices/sys_choice_case_priority.xml -> Low, Medium, High, Critical
        and the assigned-folder description establishes that new cases default
        to `Medium` priority.

    This dictionary entry ONLY declares that the `priority` Choice field exists
    on the parent table. The choice values themselves (Low, Medium, High,
    Critical) are authored as separate sys_choice records in
    ../choices/sys_choice_case_priority.xml. This separation is a deliberate
    consequence of the ServiceNow Choice-field design pattern: a sys_dictionary
    row declares the column shape; sibling sys_choice rows declare the allowed
    string-label values keyed by `name` + `element` lookup against this entry.

    Semantic source-side reference (read-only, NOT byte-for-byte ported):
      - acm-plugins/acm-default-plugins/acm-case-file-plugin/src/main/java/
        com/armedia/acm/plugins/casefile/model/CaseFile.java line 178-179:
            @Column(name = "cm_case_priority")
            private String priority;
        ArkCase's `priority` was a free-form String column on the
        `acm_case_file` JPA entity - any caller could write any string and
        the JPA layer enforced no enumeration. The ServiceNow target
        constrains the field to a controlled vocabulary of four labels
        through the `<choice>3</choice>` setting plus the sibling
        sys_choice records. This prevents typos, enables consistent
        Manager View dashboard analytics, and keeps the priority
        semantics deterministic across internal forms, the Table API,
        the scripted REST submission endpoint, and seed scripts.
      - The field name is `priority` (lowercase, 8 chars; NOT `casePriority`,
        NOT `case_priority`) per AAP Section 0.5.7 verbatim. This matches
        the conventional ServiceNow Choice-field naming convention across
        the platform (incident.priority, sc_request.priority,
        change_request.priority).

    Constraints honored (AAP Sections 0.7.1, 0.7.2, 0.7.4):
      - Scope-namespace exclusivity: `<name>` resolves to `x_casemgmt_case`
        within the scoped application, and `<sys_scope>` / `<sys_package>`
        bind THIS dictionary record to the same scope through the scope
        record id the platform serialized into both elements. NO
        global-scope writes - the OOB tables `task`, `incident`,
        `sys_user`, etc. are never modified.
      - No cross-reference sys_id was hand-authored here, and the ids that
        ARE present are the platform's own. The Phase 1 rebuild resolved
        the scope by query (`sys_scope?scope=x_casemgmt`) and let
        ServiceNow serialize every reference, so what it wrote is literal,
        not a by-name token: `<sys_package>` carries
        display_value="x_casemgmt Case Management" and source="x_casemgmt"
        over the text node 82b99028936f74320d74d6f88357a5af, and
        `<sys_scope>` carries display_value only over that same
        scope-record id, with no source attribute at all. Both text nodes
        are that id. This is ServiceNow's native serialization of a
        reference field and is AUTHORIZED by the refine PR's
        native-capture directive, which supersedes the hand-authored
        origin for this path (docs/refine-run/PHASE1-REBUILD.md).
      - The `<sys_id>` text-node value f0daac5e938b435009aa70d19dba1015
        is the identity the platform assigned this record when it created
        it: this record's own primary key, not a reference to any other
        record, and NOT derived from the field identifier
        `x_casemgmt_case.priority`. Re-running the rebuild would yield a
        different id, so this file is NOT reproducible byte-for-byte by
        regeneration; the no-hard-coded-sys_id rule is satisfied by
        PROVENANCE (nothing here was composed by hand) rather than by the
        absence of literal ids. Re-import matches the existing row on this
        id, so the captured value must be preserved verbatim: re-importing
        this XML on the same or a future PDI then idempotently updates
        that one dictionary row instead of creating a duplicate.
      - The concrete scope identifier `casemgmt` is used per AAP convention;
        ServiceNow Update Set imports use a standard XML parser, so the
        scope id must be concrete in the XML before export.
      - mandatory=false at the dictionary level per AAP Section 0.5.7
        verbatim ("priority | Choice | Low, Medium, High, Critical" -
        no Mandatory marker) and AAP Section 0.7.1 strict-literal
        "no additions, no renames, no type relaxations" rule. The
        AAP authoritative text takes precedence over any earlier
        rationalization for adding a constraint; this dictionary entry
        therefore matches the AAP literal. With `default_value=Medium`
        every newly inserted case row still receives a non-empty
        priority value automatically (the platform applies the default
        on insert), so the Manager View "All cases by priority" bar
        chart per ../reports/x_casemgmt_all_cases_by_priority.xml
        continues to render with no "(none)" bucket on synthetic seed
        data. The Choice can still be re-set to Low / High / Critical
        by the case_manager or assigned case_agent through the standard
        form before save. If a future requirement makes priority
        functionally mandatory, AAP Section 0.5.7 will be updated first
        and this dictionary entry will follow.
      - read_only=false: an internal user (case_manager or assigned
        case_agent within the AAP Section 0.5.6 "Assigned only" condition)
        editing the case record may re-prioritize a case as new
        information emerges (e.g., a Low priority complaint that
        escalates to Critical). The read_only=false setting permits the
        write through the standard form, the Table API, and the seed
        scripts; the Experience Portal lookup page does NOT expose this
        field per AAP Section 0.7.4 ("the lookup page returns ONLY
        status, subject, opened_date - no internal fields exposed").
      - default_value=Medium: explicit Title Case per the agent prompt's
        Phase 4 verbatim. The PDI's auto-numbered case-number generator
        (CASE0000001 format from ../numbers/sys_number_x_casemgmt_case
        .xml) plus this default value together let a portal-originated
        anonymous submission complete the entire mandatory field set
        (subject, type, description, requester_name, requester_email,
        priority, status) using the user-supplied free-text inputs from
        AAP Section 0.4.4 PLUS server-side defaults for the categorical
        and lifecycle fields - WITHOUT exposing priority on the public
        portal page (per AAP Section 0.7.4 minimal exposure principle).
        The scripted REST endpoint
        ../portal/rest/sys_ws_definition_x_casemgmt_case_submit.xml
        therefore does NOT need to set this field explicitly; the
        platform applies the default at insert time.
      - audit=false: the field tracks a categorical attribute, not a
        regulated lifecycle identifier. Per AAP Section 0.7.2 this POC
        is scoped to functional capability, not full audit-trail
        compliance. The `status` field in this same scoped table
        (../dictionary/x_casemgmt_case_status.xml) IS audited because
        every state transition (Draft -> Open -> In Progress -> ...)
        must be reconstructable for forensic correctness; priority,
        being a re-prioritization rather than a regulated lifecycle
        event, does not require that level of historical fidelity for
        the POC scope.
      - max_length 40 is sufficient for the longest defined label
        "Critical" (8 chars) with substantial headroom for future
        extensible labels (e.g., "Severe", "Emergency") - though AAP
        Section 0.5.7's enumeration is the authoritative four-value
        set for the POC.
      - choice 3 = "Choice list, no description column" - the standard
        Choice-field configuration when a sibling sys_choice record set
        defines the allowed values without a long-form description
        column on the dropdown.
      - No reference target: this is a String-backed Choice field, not
        a Reference field. All `<reference*>` elements are empty and
        `<table_reference>false</table_reference>`.
      - text_index=false: priority is a low-cardinality enumerated
        Choice (four values), so full-text indexing would be wasteful.
        The platform's standard list/report/dashboard surface filters
        on the column directly via GlideRecord addQuery without needing
        a full-text index.
      - No PII: the field stores only categorical urgency labels chosen
        from a controlled vocabulary; no real names, email addresses, or
        organizational identifiers are derived from or stored in this
        column.

    Cross-references (AAP Section 0.5.2 dependency ordering):
      - This record MUST load AFTER `../tables/x_casemgmt_case.xml`
        (parent table), since `<name>x_casemgmt_case</name>` references
        the table by name.
      - This record MUST load BEFORE the consumers that read or write
        the `priority` column:
          * ../choices/sys_choice_case_priority.xml (the choice values
            Low, Medium, High, Critical - which reference this
            dictionary entry by `name` + `element` lookup; the choice
            ORDER drives dropdown UX from least-to-most urgent)
          * ../reports/x_casemgmt_all_cases_by_priority.xml (Manager
            View bar chart grouped by this field per AAP Section 0.4.1)
          * ../seed-data/cases/x_casemgmt_case_demo_*.xml (synthetic
            demo cases that populate this field across the four
            priority values to exercise the Manager View bar chart
            and demonstrate the Choice-field behavior on the
            internal form)
-->
<record_update>
    <sys_dictionary action="INSERT_OR_UPDATE" element="priority" table="x_casemgmt_case">
        <active>true</active>
        <array>false</array>
        <attributes/>
        <audit>false</audit>
        <calculation><![CDATA[(function calculatedFieldValue(current) {

	// Add your code here
	return '';  // return the calculated value

})(current);]]></calculation>
        <choice>3</choice>
        <choice_field/>
        <choice_table/>
        <column_label>Priority</column_label>
        <comments>Optional Choice categorizing case urgency per AAP Section 0.5.7 verbatim (no Mandatory marker). Values: Low, Medium, High, Critical. Default: Medium - so every case row receives a non-empty value at insert time without a mandatory constraint. Used by ../reports/x_casemgmt_all_cases_by_priority.xml (Manager View dashboard bar chart). Replaces ArkCase CaseFile.priority (free-form String).</comments>
        <create_roles/>
        <default_value>Medium</default_value>
        <defaultsort/>
        <delete_roles/>
        <dependent/>
        <dependent_on_field/>
        <display>false</display>
        <dynamic_creation>false</dynamic_creation>
        <dynamic_creation_script/>
        <dynamic_default_value/>
        <dynamic_ref_qual/>
        <element>priority</element>
        <element_reference>false</element_reference>
        <foreign_database/>
        <formula/>
        <function_definition/>
        <function_field>false</function_field>
        <internal_type display_value="String">string</internal_type>
        <mandatory>false</mandatory>
        <max_length>40</max_length>
        <name>x_casemgmt_case</name>
        <next_element/>
        <primary>false</primary>
        <read_only>false</read_only>
        <read_roles/>
        <reference/>
        <reference_cascade_rule/>
        <reference_floats>false</reference_floats>
        <reference_key/>
        <reference_qual/>
        <reference_qual_condition/>
        <reference_type/>
        <spell_check>false</spell_check>
        <staged>false</staged>
        <sys_class_name>sys_dictionary</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-09-02 19:14:05</sys_created_on>
        <sys_id>f0daac5e938b435009aa70d19dba1015</sys_id>
        <sys_name>Priority</sys_name>
        <sys_package display_value="x_casemgmt Case Management" source="x_casemgmt">82b99028936f74320d74d6f88357a5af</sys_package>
        <sys_policy/>
        <sys_scope display_value="x_casemgmt Case Management">82b99028936f74320d74d6f88357a5af</sys_scope>
        <sys_update_name>sys_dictionary_x_casemgmt_case_priority</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-09-02 19:14:05</sys_updated_on>
        <table_reference>false</table_reference>
        <text_index>false</text_index>
        <unique>false</unique>
        <use_dependent_field>false</use_dependent_field>
        <use_dynamic_default>false</use_dynamic_default>
        <use_reference_qualifier>simple</use_reference_qualifier>
        <virtual>false</virtual>
        <virtual_type>script</virtual_type>
        <widget/>
        <write_roles/>
        <xml_view>false</xml_view>
    </sys_dictionary>
</record_update>
