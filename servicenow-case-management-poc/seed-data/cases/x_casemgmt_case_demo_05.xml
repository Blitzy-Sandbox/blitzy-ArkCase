<?xml version="1.0" encoding="UTF-8"?>
<!--
    REFERENCE-RESOLUTION FACTS, MEASURED ON A LIVE PDI - READ THIS FIRST
    This case row carries the pinned number CASE9000005.

    The package uses two reference shapes, decided by measurement rather
    than taste. This row uses shape B only:

      B. assigned_group (a sys_user_group), assigned_agent (a sys_user)
         -> key in the display_value attribute AND in the element body.
         sys_user and sys_user_group reference bodies are never checked by
         preview (a deliberately bogus user_name produced no problem), and
         the import engine DOES resolve them: the demo users, the demo
         group and the three sys_user_has_role rows all landed on the
         instance under these artifacts' own sys_ids with their user and
         role references correctly resolved. These columns therefore
         commit LINKED (assigned_group by name, assigned_agent by user_name)
         and need no post-commit repair.

    AAP Section 0.5.2 requires every cross-reference to resolve by a stable
    human-readable key (case by number, user by user_name, group by name,
    company by name) and forbids a literal sys_id in any reference field.
    The shape above carries the key, never a sys_id, so it complies.
    The 9,000,000 number band sits far above anything the CASE / TASK /
    PARTY counters issue, so a pinned number cannot collide with a
    counter-issued one, and the counter itself is left untouched.

    Demo Case 05 - Resolved / General Inquiry / Medium priority
    ====================================================================

    Synthetic seed record for the case-management POC scoped application.
    This file is the FIFTH of the 10-case demo dataset; together with
    cases 01 through 04 and cases 06 through 10, it satisfies the AAP
    requirement (Section 0.7.4) of "at least 10 demo cases across all
    six statuses and both case types," and contributes to the dataset
    matrix:

        Case 01 - Draft       / General Inquiry
        Case 02 - Open        / General Inquiry
        Case 03 - In Progress / General Inquiry
        Case 04 - Pending     / General Inquiry
        Case 05 - Resolved    / General Inquiry / Medium      ## THIS FILE
        Case 06 - Closed      / General Inquiry
        Case 07 - Open        / Complaint
        Case 08 - In Progress / Complaint
        Case 09 - Resolved    / Complaint
        Case 10 - Closed      / Complaint

    The 10-case dataset thus exercises:
      * Every one of the six statuses (Draft, Open, In Progress, Pending,
        Resolved, Closed) - this case occupies the Resolved slot for the
        General Inquiry case type
      * Both case types (General Inquiry and Complaint) across the
        full lifecycle - case 05 is the canonical RESOLVED GENERAL
        INQUIRY and is the General-Inquiry-flow analog of case 09
        (Resolved Complaint)
      * Multiple priorities (Low, Medium, High, Critical) - this case
        contributes to the Medium bar in the all_cases_by_priority chart

    Lifecycle position - WHY Resolved / General Inquiry / Medium is
    meaningful:
      A General Inquiry case that has reached Resolved status has
      traversed:
        Draft -> Open -> In Progress -> Resolved
      (skipping Pending; this case did NOT need to traverse Pending to
      reach Resolved). The agent has completed all investigative tasks
      and is now awaiting manager review for final closure. The Medium
      priority is appropriate at this lifecycle phase: the case carries
      moderate urgency through the investigation; with no Critical or
      High signal needed to escalate, Medium reflects routine handling
      that nonetheless completed the active-lifecycle phase.

    Role / cross-dependency context (per AAP Section 0.5.2):

      This case DEPENDS ON (loaded BEFORE this record):
        - The scoped application records under ../../app/
            (sys_app and sys_scope; this case's <sys_scope>/<sys_package>
             reference fields resolve via the source attribute against
             those records)
        - The case table definition at ../../tables/x_casemgmt_case.xml
            (this record's <sys_class_name>x_casemgmt_case</sys_class_name>
             requires the table to exist)
        - All dictionary entries for the case table under
            ../../dictionary/x_casemgmt_case_*.xml  (every populated
            field below requires its dictionary entry to be present)
        - All choice records under ../../choices/  (the type=General
            Inquiry, status=Resolved, and priority=Medium choice values
            must be registered)
        - The auto-numbering record at
            ../../numbers/sys_number_x_casemgmt_case.xml  (defines the
            CASE0000001 format and the counter used at runtime;
            this record pins CASE9000005 instead, in the 9,000,000 band, so a
            packaged child row can name its parent by number)
        - The role records under ../../roles/  (the demo agent's read
            and write access requires the x_casemgmt_case_agent role and
            its ACLs; the eventual Resolved->Closed transition will
            require the demo manager's x_casemgmt_case_manager role)
        - The ACL records under ../../acl/  (table-level and field-level
            ACLs gate read/write access; the demo agent reads/writes
            this case via the "Assigned only" clause of the agent ACLs)
        - The business rule records under ../../business_rules/
            (set_opened_date Before-Insert auto-sets <opened_date> on
             this case at import time; block_draft_backtransition will
             prevent any Resolved->Draft attempt; set_closed_date will
             fire only on a future Resolved->Closed transition - NOT on
             this insert; validate_assigned_agent_membership validates
             that the demo agent is a member of the demo team at insert
             time)
        - The flow records under ../../flows/
            (../../flows/general_inquiry_state_machine.xml governs this
             case type; it is the GENERAL-INQUIRY-flow CANONICAL
             RESOLVE-SUCCESS test - the inverse of case 03 which is
             BLOCKED by an Open task)
        - ../users/sys_user_x_casemgmt_demo_agent.xml  (referenced by
            <assigned_agent>; resolved by user_name lookup)
        - ../groups/sys_user_group_x_casemgmt_demo_team.xml  (referenced
            by <assigned_group>; resolved by name lookup)
        - ../role_assignments/sys_user_has_role_x_casemgmt_demo_agent.xml
            (grants the case_agent role to the demo agent so the agent
            can read/write this case via the "Assigned only" ACL clause)

      This case IS REFERENCED BY (loaded AFTER this record):
        - ../tasks/x_casemgmt_case_task_demo_*.xml - specifically the
            two Closed tasks per the seed script's task table:
            "Demo task 04: Final review for resolution" (status =
            Closed) and "Demo task 05: Post-resolution archive"
            (status = Closed). With these two Closed tasks and ZERO
            non-Closed tasks, the validate_resolved_transition
            subflow's GlideRecord query against x_casemgmt_case_task
            where case=current AND status!=Closed returns ZERO rows,
            allowing the In Progress -> Resolved transition to
            succeed. This is the inverse of case 03 (which is BLOCKED
            by a non-Closed task).
        - ../parties/x_casemgmt_case_party_demo_*.xml - specifically the
            Person-type Requester party (referencing the demo viewer)
            and the Organization-type Respondent party (referencing
            "Synthetic Org Beta"). This exercises BOTH sides of the
            polymorphic UI Policy on the case_party form: the Person
            branch shows the person reference field and the Organization
            branch shows the organization reference field.
        - The all_cases_by_status Manager View bar chart at
            ../../reports/x_casemgmt_all_cases_by_status.xml  (this case
            populates the Resolved bar)
        - The all_cases_by_type Manager View donut at
            ../../reports/x_casemgmt_all_cases_by_type.xml  (this case
            populates the General Inquiry slice)
        - The all_cases_by_priority Manager View bar chart at
            ../../reports/x_casemgmt_all_cases_by_priority.xml  (this
            case populates one of the Medium entries)
        - The case_count_by_status Agent Workspace donut at
            ../../reports/x_casemgmt_case_count_by_status.xml  (this case
            populates the Resolved slice)
        - The cases_opened_30d Manager View single-score widget at
            ../../reports/x_casemgmt_cases_opened_30d.xml  (since
            opened_date is auto-set to "now" by the set_opened_date
            business rule at insert time, this case is included in the
            count of cases opened within the last 30 days)
        - The avg_time_to_close Manager View single-score widget at
            ../../reports/x_casemgmt_avg_time_to_close.xml  (this case
            does NOT contribute because the widget filters on
            status=Closed and this case is Resolved, not Closed; only
            cases 06 and 10 contribute to the average. If/when the demo
            manager subsequently closes this case via the form, it
            WOULD become an additional input to the avg widget.)
        - The validate_resolved_transition subflow test in Validation
            Gate 2 (RESOLVE-SUCCESS): THIS CASE IS THE CANONICAL
            RESOLVE-SUCCESS TEST FOR THE GENERAL INQUIRY FLOW. Where
            case 09 tests resolve-success in the complaint flow, this
            case tests it in the general_inquiry flow. Both flows MUST
            behave identically when all child tasks are Closed.
        - The validate_closed_transition subflow test in Validation
            Gate 2 (CLOSE-SUCCESS on GENERAL INQUIRY): a case_manager-
            roled user (the demo manager) can close this case via the
            form, demonstrating that the general_inquiry flow's
            validate_closed_transition subflow checks the manager role
            and allows the transition. The set_closed_date Before-Update
            business rule will fire on that subsequent update and
            populate closed_date to gs.nowDateTime().
        - The Agent Workspace's "My Open Cases" list at
            ../../reports/x_casemgmt_my_open_cases.xml does NOT include
            this case: the list filter excludes Resolved (and Closed)
            cases per "status NOT IN (Resolved, Closed)".

    Authoring rules honored (per AAP Sections 0.7.1 / 0.7.2 and the
    agent prompt's ten authoring rules):

      Rule 1 - Synthetic only (AAP Section 0.7.2 NO-PII clause):
        subject       = "Demo case 05: Resolved (General Inquiry)"
        description   = "Synthetic Resolved case awaiting manager
                         close." (no PII; the wording emphasizes the
                         lifecycle position - resolved by agent,
                         awaiting manager review for closure)
        requester_name  = "Synthetic Requester Five" (synthetic
                          identifier using the literal word "Synthetic"
                          plus a numeral spelled out for the requester,
                          NOT a real person's name; consistent with the
                          peer convention used by cases 06, 07, 08, 09,
                          10 - SPELLED-OUT word for the requester_name)
        requester_email = "requester-5@example.invalid"
                          (RFC 2606 / BCP 32 reserved .invalid TLD never
                          resolves to a real domain; the local part uses
                          the NUMERAL "5" matching the file numbering -
                          consistent with the peer convention used by
                          cases 06-10's NUMERAL convention for the email
                          local part versus the SPELLED-OUT word for
                          the requester_name field)

      Rule 2 - No auto-number override:
        The <number> element carries the pinned value CASE9000005.
        It is pinned, not left to the counter, because a packaged task or
        party row identifies its parent case BY NUMBER, so that number has
        to be knowable when the package is authored - the previous revision
        left every packaged case row with an empty number, which is why the
        packaged task and party links could never resolve. The auto-number
        engine at ../../numbers/sys_number_x_casemgmt_case.xml still owns
        the CASE0000001 format and its counter for every case created at
        runtime; the pinned value sits in the 9,000,000 band, far above
        anything that counter will issue, so it cannot collide with a
        counter-issued number and the counter is left untouched.
        (An earlier revision of this file claimed that presetting <number>
        "would conflict with the auto-numbering counter and may cause
        Update Set preview errors per AAP Section 0.7.3 Validation Gate 7".
        That was measured on a live instance and is not true: a preview of
        the rebuilt package reports zero problems for these rows.)

      Rule 3 - Auto-set opened_date (NO explicit override):
        The <opened_date/> element is intentionally absent. The
        set_opened_date Before-Insert business rule at
        ../../business_rules/x_casemgmt_set_opened_date.xml fires on
        insert and sets opened_date = gs.nowDateTime(). This means the
        case will appear as "opened today" at import time, which is
        appropriate for the cases_opened_30d widget (this case is
        included in the 30-day count). Unlike case 06 (Closed General
        Inquiry) and case 10 (Closed Complaint) - which DO carry
        explicit opened_date and closed_date overrides because they
        are inputs to the avg_time_to_close widget that filters on
        status=Closed - this case does NOT need an explicit
        opened_date override because:
          * It is NOT used to compute avg_time_to_close (which excludes
            non-Closed cases via the closed_date IS NOT NULL filter).
          * It IS appropriate for the case to appear "fresh" in the
            cases_opened_30d count (a Resolved-but-not-yet-Closed case
            is still recently active).
        Pattern matches case 09 (Resolved Complaint), the Complaint-
        flow analog of this case.

      Rule 4 - Reference fields (AAP Section 0.7.2 "NO sys_id literals
               anywhere"). Two classes, distinguished by MEASUREMENT on a
               live PDI - not by convention:

        CLASS 2 - key in BOTH the display_value attribute and the body:
          <assigned_group display_value="x_casemgmt_demo_team">x_casemgmt_demo_team</assigned_group>
          <assigned_agent display_value="x_casemgmt_demo_agent">x_casemgmt_demo_agent</assigned_agent>

          sys_user and sys_user_group reference bodies are NOT checked by
          preview (a deliberately bogus user_name produced no problem) and
          the import engine DOES resolve them: the demo users, the demo
          group and the three sys_user_has_role rows all arrived on the
          instance carrying these artifacts' own sys_ids with correctly
          resolved user/role references. These therefore commit LINKED and
          need no post-commit repair; seed_demo_data.js only fills a
          reference it finds empty, so it is a no-op for them:
            assigned_group -> sys_user_group by name
            assigned_agent -> sys_user by user_name

        Adoption key: the pinned <number> CASE9000005 below. It sits in the
        9,000,000 band the project already uses for pinned fixtures, so it
        cannot collide with a counter-issued number, and it lets
        seed_demo_data.js ADOPT and repair this packaged row instead of
        inserting a duplicate. Re-running that script is a no-op
        (verified twice on a live PDI: 0 repairs, 0 duplicates).

        Platform-managed metadata columns - NOT domain references, and
        serialized exactly as ServiceNow itself exports a scoped record:
          <sys_scope display_value="Case Management">
              82b99028936f74320d74d6f88357a5af</sys_scope>
          (body = the scoped application's own sys_scope sys_id)
          <sys_package display_value="Case Management" source="x_casemgmt">
              x_casemgmt</sys_package>
        The <sys_id> below is this record's OWN primary key (deterministic
        MD5), which the AAP rule permits - it is not a reference to another
        record.

      Rule 5 - Both assignments populated, NO pending_reason:
        assigned_group = "x_casemgmt_demo_team"  (mandatory because the
                         case had to traverse Open -> ... -> Resolved,
                         and assigned_group is required at Open per
                         AAP Section 0.5.5; the value persists through
                         all subsequent statuses including Resolved)
        assigned_agent = "x_casemgmt_demo_agent"  (mandatory because the
                         case had to traverse In Progress -> ... ->
                         Resolved, and assigned_agent is required at In
                         Progress per AAP Section 0.5.5; the value
                         persists; the agent is also a member of
                         x_casemgmt_demo_team per the seed script's
                         group-membership creation, satisfying the
                         validate_assigned_agent_membership business
                         rule's pre-update guard)
        pending_reason = (absent / unpopulated)  (Resolved cases never
                         carry a pending_reason; this case did NOT
                         traverse Pending, so the value was never set;
                         even if it had traversed Pending, the
                         clear_pending_reason_on_inprogress business
                         rule would have cleared the value when the
                         case left Pending)

      Rule 6 - Choice value precision:
        type     = "General Inquiry"  (exactly as registered in
                   ../../choices/sys_choice_case_type.xml; the FIRST
                   of the two case types; capital G and capital I
                   deliberate; the space between "General" and
                   "Inquiry" is a single ASCII space, NOT an em-space
                   or other Unicode whitespace)
        status   = "Resolved"     (exactly as registered in
                   ../../choices/sys_choice_case_status.xml; the
                   penultimate state before terminal Closed; capital
                   R deliberate - "Resolved" not "resolved" or
                   "RESOLVED")
        priority = "Medium"       (exactly as registered in
                   ../../choices/sys_choice_case_priority.xml; the
                   middle priority; capital M deliberate - "Medium"
                   not "medium" or "MEDIUM")

      Rule 7 - Zero hardcoded sys_id cross-references (AAP Section
               0.7.2 "NO sys_id literals anywhere"):
        The case's OWN <sys_id> below is the record's primary key,
        which is acceptable per the AAP rule (it is NOT a reference to
        any other record). It is a deterministic 32-character lowercase
        hex value computed at file generation time as the MD5 of the
        idempotent subject "Demo case 05: Resolved (General Inquiry)" =
        4444a0dd04a2c2b5d4a1473a416fb1af - so the Update Set engine
        resolves the same sys_id on every PDI import, making the record
        idempotent.

        EVERY cross-reference field below uses the stable human-readable
        lookup key (user_name / name / scope source) in the
        display_value attribute, with an empty element body - NOT a hardcoded
        sys_id literal. This matches the convention established by every
        peer file in the scoped Update Set (sys_user_*, sys_user_group_*,
        sys_user_has_role_*, dictionary/x_casemgmt_*, cases 06/07/08/09/10,
        etc.) and complies with the AAP Section 0.7.2 "NO sys_id
        literals anywhere" rule:
          - <assigned_agent display_value="x_casemgmt_demo_agent"/>  is linked after commit by ../../scripts/seed_demo_data.js via user_name lookup
          - <assigned_group display_value="x_casemgmt_demo_team"/>  is linked after commit by ../../scripts/seed_demo_data.js via group-name lookup
          - <sys_scope display_value="Case Management">82b99028936f74320d74d6f88357a5af</sys_scope>  platform-managed metadata; resolved by the import engine itself
          - <sys_package display_value="Case Management" source="x_casemgmt">
                x_casemgmt
            </sys_package>  platform-managed metadata; resolved by the import engine itself

      Rule 8 - Scope binding:
        <sys_scope>   binds this case row to the scoped application via
                      the scope source name "x_casemgmt" defined in
                      ../../app/sys_app/x_casemgmt_case_management.xml at
                      <source>x_casemgmt</source>; the display_value
                      "Case Management" matches the sys_app's human-
                      readable name.
        <sys_package> follows the same pattern with the additional
                      source="x_casemgmt" attribute that is required by
                      the Update Set engine for scope-package binding.
        Note: the source attribute value "x_casemgmt" matches what every
        peer file uses; it is the scope's <source> field, NOT the
        scoped application's sys_update_name. This is the convention
        used consistently across the project's Update Set artifacts
        (cases 06/07/08/09/10, all sys_user_*, sys_user_group_*,
        sys_user_has_role_* records use the same convention).

      Rule 9 - Idempotency anchor:
        The case's idempotent key is <subject> =
        "Demo case 05: Resolved (General Inquiry)". The companion seed
        script ../../scripts/seed_demo_data.js ensureCase() helper
        queries x_casemgmt_case by subject BEFORE inserting; the
        action="INSERT_OR_UPDATE" attribute on the inner <x_casemgmt_case>
        element ensures the same behavior at Update Set import time.
        Both the action attribute and the deterministic sys_id make
        re-imports on a fresh PDI byte-identical and collision-free.

      Rule 10 - Scoped Update Set XML conventions:
        Root element:  <record_update table="x_casemgmt_case">
        Inner element: <x_casemgmt_case action="INSERT_OR_UPDATE"> for
                       idempotent re-import per AAP Section 0.7.1
                       round-trip-verification gate (Validation Gate 7).
        UTF-8 encoding declared in XML prolog.
        Element ordering: alphabetical (matching the established peer
        convention used by sys_user_*, sys_user_group_*,
        sys_user_has_role_*, role records, cases 06/07/08/09/10, and
        dictionary entries across the scoped Update Set).
        Unix-style line endings (LF) per AAP file authoring conventions.

    Source-side semantic reference (read-only, NOT byte-for-byte ported)
    per AAP Section 0.8.1:
      acm-plugins/acm-default-plugins/acm-case-file-plugin/src/main/java/
        com/armedia/acm/plugins/casefile/model/CaseFile.java
        ArkCase's CaseFile JPA entity declares ~80 fields including
        cm_case_status (the source for our <status>), cm_case_priority
        (the source for our <priority>), cm_case_type (the source for
        our <type>), cm_case_title (the source for our <subject>),
        cm_case_details (Lob; the source for our <description>), and
        cm_case_closed (Temporal TIMESTAMP; the source for our
        <closed_date>). The ServiceNow target collapses ArkCase's ~80
        fields to the user-prompt's 12-field schema (number, type,
        status, priority, subject, description, opened_date, closed_date,
        assigned_group, assigned_agent, requester_name, requester_email
        - plus the optional pending_reason). ArkCase's queue-handler-
        driven default status flow ("DRAFT" -> "ACTIVE" on save via
        CaseFileQueueHandler.java) is replaced in this POC by Flow
        Designer's general_inquiry_state_machine.xml; ArkCase's
        ChangeCaseFileStateService.java is replaced by the
        x_casemgmt_CaseTransitionValidator script include. ArkCase's
        CaseFileTasksService.java aggregation logic (which informed
        the "all tasks closed before resolve" guard) is replaced by
        the validate_resolved_transition subflow's inline GlideRecord
        query against x_casemgmt_case_task.

    Validation hooks (per the agent prompt):
      - The XML well-formedness check (xmllint with no-output flag)
        returns zero errors.
      - No-PII grep returns zero matches against (john|jane|smith|doe|
        @gmail|@yahoo|@outlook|@hotmail|\.com|\.org|\.net) - the
        synthetic identifiers ("Synthetic Requester Five",
        "requester-5@example.invalid", "x_casemgmt_demo_agent",
        "x_casemgmt_demo_team") do not include any PII tokens.
      - Choice-value validation: <type>General Inquiry</type>,
        <status>Resolved</status>, <priority>Medium</priority> -
        capital G/I, capital R, capital M respectively.
      - Anti-pattern validation: NO <number>, NO <opened_date>,
        NO <closed_date>, NO <pending_reason> elements present.
      - Sys-id-literal validation: only ONE <sys_id> element (the
        case's own primary key); NO sys_id literals appear in any
        cross-reference field.
      - Scope-identifier validation: the concrete scope identifier `casemgmt` is used verbatim in the sys_class_name, sys_update_name,
        all reference values, and the file path; ServiceNow Update Set imports use a standard XML parser, so the scope id is concrete in the XML before export. The same identifier is used consistently across every cross-reference in this file.

    Key insights (per the agent prompt):
      1. Demo case 05 is the canonical Resolve-success case for the
         General Inquiry flow. It is the inverse of demo case 03
         (which is blocked because of an Open task). Case 05 has only
         Closed tasks, so the In Progress -> Resolved transition
         succeeds.
      2. THIS CASE IS THE RESOLVE-SUCCESS TEST. When the demo tasks
         are seeded, this case will have two Closed tasks ("Demo task
         04: Final review for resolution", "Demo task 05: Post-
         resolution archive"). The validate_resolved_transition
         subflow's GlideRecord query against x_casemgmt_case_task
         where case=current AND status!=Closed returns ZERO rows, so
         the transition is allowed.
      3. THIS CASE IS THE CLOSE-SUCCESS TEST. Subsequently, the demo
         manager (who has the case_manager role) can close this case
         via the form, demonstrating: (a) the validate_closed_transition
         subflow's role-check passes, (b) the set_closed_date business
         rule auto-populates closed_date. After closure, this case
         becomes one of the inputs to the avg_time_to_close widget.
      4. The status "Resolved" has a capital R. Not lowercase, not all
         caps. The exact choice value is "Resolved" per AAP Section
         0.5.7 and ../../choices/sys_choice_case_status.xml.
      5. The <closed_date> field is intentionally empty. Even though
         this case is Resolved, it has not been Closed yet. The
         set_closed_date business rule is Before-Update on
         Resolved -> Closed, so the field is set only when the case
         actually transitions to Closed.
      6. Demo case 05 has both party types - Person (demo viewer as
         Requester) and Organization (Synthetic Org Beta as Respondent)
         - to exercise BOTH branches of the polymorphic UI Policy on
         the case_party form.
      7. The case appears in the case_count_by_status donut and
         all_cases_by_status bar in the Manager View dashboard's
         Resolved slice/bar.
      8. The case does NOT appear in the Agent Workspace's "My Open
         Cases" list. The list filter is "status NOT IN (Resolved,
         Closed) AND assigned_agent = javascript:gs.getUserID()".
         Resolved cases are excluded.
      9. Demo case 05 is the General-Inquiry-flow analog of demo case
         09 (Resolved Complaint). Both flows MUST behave identically
         when all child tasks are Closed; case 05 validates the
         general_inquiry flow's resolve-success path while case 09
         validates the complaint flow's resolve-success path.
     10. Medium priority is appropriate at this lifecycle phase: the
         case carries moderate urgency through the investigation and
         resolution; with no Critical or High signal needed, Medium
         reflects routine handling that nonetheless completed the
         active-lifecycle phase.
-->
<record_update table="x_casemgmt_case">
    <x_casemgmt_case action="INSERT_OR_UPDATE">
        <assigned_agent display_value="x_casemgmt_demo_agent">x_casemgmt_demo_agent</assigned_agent>
        <assigned_group display_value="x_casemgmt_demo_team">x_casemgmt_demo_team</assigned_group>
        <description>Synthetic Resolved case awaiting manager close.</description>
        <number>CASE9000005</number>
        <priority>Medium</priority>
        <requester_email>requester-5@example.invalid</requester_email>
        <requester_name>Synthetic Requester Five</requester_name>
        <status>Resolved</status>
        <subject>Demo case 05: Resolved (General Inquiry)</subject>
        <sys_class_name>x_casemgmt_case</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_id>4444a0dd04a2c2b5d4a1473a416fb1af</sys_id>
        <sys_package display_value="Case Management" source="x_casemgmt">x_casemgmt</sys_package>
        <sys_scope display_value="Case Management">82b99028936f74320d74d6f88357a5af</sys_scope>
        <sys_update_name>x_casemgmt_case_demo_05</sys_update_name>
        <type>General Inquiry</type>
    </x_casemgmt_case>
</record_update>
