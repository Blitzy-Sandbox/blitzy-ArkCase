<?xml version="1.0" encoding="UTF-8"?>
<!--
    PROVENANCE - Refine PR Phase 1 native rebuild (supersedes the hand-authored
    origin of this record; see docs/refine-run/PHASE1-REBUILD.md):
    This file is a serialization of the record the PLATFORM ITSELF captured when the
    `x_casemgmt_case_task` table
    was created through the real Table API / dictionary API. It is no longer a
    hand-authored record definition. The human's PR directive requires all table and
    role-link records to be created by native platform actions so that physical
    storage provisioning and capture side effects fire; that directive overrides the
    Agent Action Plan's CREATE-mode hand-authored origin for this path.
      Captured into Update Set : x_casemgmt_case_management v1.0.0 (native rebuild)
      Update record name       : sys_db_object_7bca2c5e938b435009aa70d19dba1095
      Update record type       : Table
      Update record target     : Case Task
      Captured (instance time) : 2026-09-02 19:14:02
    What this changes about any note below:
      - the <sys_id> element carries the identity the platform assigned at creation
        time (7bca2c5e938b435009aa70d19dba1095), not a deterministic MD5 of the field identifier;
      - the element order, the action/element/table attributes the platform places on
        the record element, and the platform-managed elements it adds (the calculation
        template, virtual_type, formula, reference_floats, ...) are its own
        serialization, not a formatting choice made here;
      - the <sys_scope>/<sys_package> elements are the platform's own serialization of
        the x_casemgmt scope reference (display_value on both, plus source="x_casemgmt"
        on sys_package only; sys_scope carries no source attribute), reproduced
        verbatim; the only sys_id this file introduces of its own accord is none - the
        rebuild resolved the scope by query (sys_scope?scope=x_casemgmt) and let the
        platform write the reference.
    Every statement about semantics, type, constraints, scope exclusivity and AAP
    traceability still holds: each value was read from the live dictionary before the
    rebuild and replayed into the platform verbatim. Only statements about HOW the
    record was authored are superseded.

    ServiceNow Update Set record-definition for the child-task
    `x_casemgmt_case_task` custom table inside the scoped application
    `x_casemgmt`.

    This table is the case-task subset of the AAP's three-table data model
    (AAP Section 0.5.7). It replaces the JPA entity AcmTask defined in
    com.armedia.acm.plugins.task.model.AcmTask
    (acm-plugins/acm-default-plugins/acm-task-plugin/src/main/java/com/
    armedia/acm/plugins/task/model/AcmTask.java), specifically the tiny
    six-field subset enumerated in AAP Section 0.5.7. The vast majority of
    AcmTask's ~60 fields (priority, attachment metadata, business-process
    info, percent-complete, workflow request IDs, document-under-review,
    documents-to-review, ECM container, participants, candidate claim
    groups, buckslip approval, rework instructions, outcome information,
    notification receivers, ad-hoc/completion flags, taskDurationInMillis,
    nextAssignee, legacySystemId, restriction flag, etc.) are intentionally
    OUT OF SCOPE per AAP Section 0.3.2 (no document management, no
    Activiti/BPMN workflow engine, no buckslip approval, no ECM, no
    business-process integration, no Outlook calendar, no ZyLAB matter
    integration, no notification engine).

    The six in-scope fields and their AcmTask source-field counterparts:
      - case        (Reference -> x_casemgmt_case)         <- attachedToObjectId + attachedToObjectType (when CASE_FILE)
      - subject     (String 255, mandatory)                <- title (with @Size(min=1) Bean Validation)
      - type        (Choice)                               <- type (free-form String in source; collapsed to choice)
      - status      (Choice: Open / In Progress / Closed)  <- status (with pendingStatus companion in source)
      - assigned_to (Reference -> sys_user, mandatory)     <- assignee (replaced username string lookup with reference)
      - due_date    (Date, mandatory)                      <- dueDate (with @NotNull)

    These six fields are NOT defined in this record - they are authored as
    separate sys_dictionary records in the sibling ../dictionary/ folder
    (../dictionary/x_casemgmt_case_task_case.xml, x_casemgmt_case_task_subject.xml,
    x_casemgmt_case_task_type.xml, x_casemgmt_case_task_status.xml,
    x_casemgmt_case_task_assigned_to.xml, x_casemgmt_case_task_due_date.xml).
    The choice value lists for type and status are authored in
    ../choices/sys_choice_case_task_type.xml (Investigation, Review,
    Follow-up, Other) and ../choices/sys_choice_case_task_status.xml (Open,
    In Progress, Closed) per AAP Section 0.5.1.

    This record authors ONLY table-level metadata (name, labels, parent
    class, scope linkage, auto-numbering reference, and access flags). The
    platform automatically inherits the standard sys_* audit columns
    (sys_id, sys_created_on, sys_created_by, sys_updated_on,
    sys_updated_by) by virtue of being a sys_db_object record.

    Constraints honored:
      - Scoped-namespace exclusivity: every cross-reference resolves into
        the x_casemgmt scope; no global-scope writes.
      - No sys_id was hand-authored in any reference field, and the ids
        that ARE present are the platform's own. The rebuild resolved the
        scope by query (sys_scope?scope=x_casemgmt) and let ServiceNow
        serialize the references, so what it wrote is literal, not a
        by-name token: <sys_package> carries
        display_value="x_casemgmt Case Management" and source="x_casemgmt"
        over the text node 82b99028936f74320d74d6f88357a5af; <sys_scope>
        carries display_value only over that same scope-record id, with no
        `source` attribute; and <number_ref> carries
        display_value="TASK" over the bare literal sys_number record id
        ee298f5e8c95d67c6fa9a97f5b390782, with no `source` attribute
        either. This is ServiceNow's native serialization of a reference
        field and is AUTHORIZED by the refine PR's native-capture
        directive, which supersedes the hand-authored origin for this path
        (../docs/refine-run/PHASE1-REBUILD.md).
      - The <sys_id> element below is the identity the platform assigned
        this record when it created it (7bca2c5e938b435009aa70d19dba1095):
        this record's own primary key, not a reference to any other record,
        and NOT derived from the table name `x_casemgmt_case_task`.
        Re-running the rebuild would yield a different id, so this file is
        not reproducible byte for byte by regeneration; the
        no-hard-coded-sys_id rule is satisfied by PROVENANCE (nothing here
        was composed by hand) rather than by the absence of literal ids.
        Re-import matches the existing row on this id, which is why the
        captured value must be preserved verbatim.
      - super_class is EMPTY: this table extends NOTHING. Critically, it
        does NOT extend the OOB `task` table even though the source entity
        is named AcmTask and the conceptual mapping aligns with task
        semantics. Extending the OOB `task` table would (a) write to global
        scope and (b) inherit the OOB task table's ~60 columns (priority,
        state, due_date, etc.), creating massive scope creep and violating
        AAP Section 0.3.2's MINIMAL CHANGE CLAUSE. The platform still
        provides the standard sys_* audit columns because sys_db_object
        records always do.
      - extension_model is empty: this is a free-standing user-data table,
        not an extended table.
      - is_extendable is false: this POC table is not designed to be
        sub-classed.
      - create_access_controls is false: ServiceNow auto-creates a default
        ACL set when this is true. Since ../acl/x_casemgmt_case_task_*.xml
        enumerates a precise role x table x CRUD matrix that mirrors the
        case-table ACL pattern (case_manager full access; case_agent
        restricted to tasks whose parent case has the agent assigned;
        case_viewer read-only), the auto-generated ACLs would conflict and
        are explicitly suppressed here.
      - The reference target sys_user (for `assigned_to`) is an OOB
        ServiceNow table that this record does NOT modify. It is referenced
        ONLY via the dictionary entry in
        ../dictionary/x_casemgmt_case_task_assigned_to.xml, never via
        super_class.
      - The concrete scope identifier casemgmt is used throughout per AAP convention; ServiceNow Update Set imports use a standard XML parser, so the scope id is concrete in the XML before export. The one
        source="x_casemgmt" attribute the platform emitted sits on
        <sys_package>; <sys_scope> and <number_ref> carry no source
        attribute, so on import those two references are matched by the
        literal sys_id text node they hold, and this record itself is
        matched by its own captured sys_id. Portability therefore rests on
        the referenced scope, package and sys_number rows arriving with
        those same ids (they travel in this same Update Set), not on a
        by-name lookup.
      - CROSS-SCOPE ACCESS IS LEAST PRIVILEGE: READ ONLY, AND THE COLUMNS ARE
        BOOLEAN, NOT STRINGS. On sys_db_object only `access` is a string
        (public | package_private); ws_access, read_access, create_access,
        update_access, delete_access, alter_access, client_scripts_access and
        configuration_access are `boolean`. An earlier revision declared all of
        them as the string "public", which a boolean column coerces to FALSE, so
        the platform refused every cross-scope operation on this table ("Read
        operation against 'x_casemgmt_case_task' from scope 'rhino.global' has
        been refused due to the table's cross-scope access policy"). That broke
        the REST Table API verification gate, every global-scope verification
        script, and the ATF client test runner, whose Global-scope
        TestExecutorAjax resolves form records with a plain GlideRecord - all
        three of which need only READ. read_access and ws_access are therefore
        emitted as boolean true below and create_access, update_access and
        delete_access as boolean FALSE: Application Access is a separate gate
        from ../acl/*.xml (plain GlideRecord in another scope is not filtered by
        record ACLs, and Global code can suppress this application's
        before-update guards with setWorkflow(false)), so an open write column
        let any Global caller mutate this table outside the role matrix of AAP
        Section 0.5.6. alter_access, client_scripts_access and
        configuration_access stay explicitly FALSE. See
        ../tables/x_casemgmt_case.xml for the full measurement record, the
        evidence that no capability depends on cross-scope writes, and the
        syscache_tabledescriptor cache note.

    Cross-Update-Set load ordering (per AAP Section 0.5.2):
      - This record MUST be loaded AFTER ../app/sys_app/x_casemgmt_case_management.xml and
        ../app/sys_app/x_casemgmt_case_management.xml.
      - This record MUST be loaded AFTER
        ../numbers/sys_number_x_casemgmt_case_task.xml (referenced by
        number_ref).
      - This record MUST be loaded AFTER ../tables/x_casemgmt_case.xml
        because ../dictionary/x_casemgmt_case_task_case.xml (loaded after
        this record) creates a Reference column whose target is
        x_casemgmt_case.
      - This record MUST be loaded BEFORE all
        ../dictionary/x_casemgmt_case_task_*.xml records (they reference
        this table by `name`).
      - This record MUST be loaded BEFORE all
        ../acl/x_casemgmt_case_task_*.xml records.
      - This record MUST be loaded BEFORE the case state-machine flows in
        ../flows/general_inquiry_state_machine.xml and
        ../flows/complaint_state_machine.xml because the
        validate_resolved_transition subflow performs a GlideRecord query
        against this table to enforce the "All tasks must be closed before
        resolving this case." rule (AAP Section 0.7.4 verbatim).
      - This record MUST be loaded BEFORE the
        ../script_includes/x_casemgmt_CaseTransitionValidator.xml Script
        Include because it queries this table.
      - This record MUST be loaded BEFORE
        ../reports/x_casemgmt_my_overdue_tasks.xml (the report queries
        x_casemgmt_case_task by due_date and assigned_to).
      - This record MUST be loaded BEFORE all
        ../seed-data/tasks/x_casemgmt_case_task_demo_*.xml records.
-->
<record_update table="sys_db_object">
    <sys_db_object action="INSERT_OR_UPDATE">
        <access>public</access>
        <actions_access>false</actions_access>
        <alter_access>false</alter_access>
        <caller_access/>
        <client_scripts_access>false</client_scripts_access>
        <configuration_access>false</configuration_access>
        <create_access>false</create_access>
        <create_access_controls>false</create_access_controls>
        <delete_access>false</delete_access>
        <filter_extension/>
        <is_df_table>false</is_df_table>
        <is_extendable>false</is_extendable>
        <label>Case Task</label>
        <live_feed_enabled>false</live_feed_enabled>
        <name>x_casemgmt_case_task</name>
        <number_ref display_value="TASK">ee298f5e8c95d67c6fa9a97f5b390782</number_ref>
        <provider_class/>
        <read_access>true</read_access>
        <scriptable_table>false</scriptable_table>
        <super_class/>
        <sys_class_name>sys_db_object</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-09-02 19:14:01</sys_created_on>
        <sys_id>7bca2c5e938b435009aa70d19dba1095</sys_id>
        <sys_mod_count>2</sys_mod_count>
        <sys_name>Case Task</sys_name>
        <sys_package display_value="x_casemgmt Case Management" source="x_casemgmt">82b99028936f74320d74d6f88357a5af</sys_package>
        <sys_policy/>
        <sys_scope display_value="x_casemgmt Case Management">82b99028936f74320d74d6f88357a5af</sys_scope>
        <sys_update_name>sys_db_object_7bca2c5e938b435009aa70d19dba1095</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-09-02 19:15:46</sys_updated_on>
        <update_access>false</update_access>
        <user_role/>
        <ws_access>true</ws_access>
    </sys_db_object>
</record_update>
